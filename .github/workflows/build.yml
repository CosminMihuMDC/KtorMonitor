name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4096M -Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Dorg.gradle.caching=true"

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run unit tests
        run: ./gradlew jvmTest

#      - name: Upload test reports
#        uses: actions/upload-artifact@v7
#        with:
#          name: test-reports
#          path: "**/build/reports/tests/"

  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build Android APKs
        run: ./gradlew :sample:ktor:androidApp:assembleDebug :sample:ktor:androidApp:assembleRelease :sample:okhttp:androidApp:assembleDebug :sample:okhttp:androidApp:assembleRelease :sample:http4k:androidApp:assembleDebug :sample:http4k:androidApp:assembleRelease

#      - name: Upload Android APKs
#        uses: actions/upload-artifact@v7
#        with:
#          name: android-apks
#          path: "**/build/outputs/apk/**/*.apk"

#  build-ios:
#    name: Build iOS simulator app
#    runs-on: macos-latest
#    needs: test
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v6
#
#      - name: Build iOS simulator app
#        run: |
#          xcodebuild build \
#          -project sample/ktor/iosApp/iosApp.xcodeproj \
#          -configuration Debug \
#          -scheme ios \
#          -sdk iphonesimulator \
#          -arch arm64 \
#          -derivedDataPath ./build \
#          -verbose

#      - name: Upload App Folder
#        uses: actions/upload-artifact@v7
#        with:
#          name: iphonesimulator-app
#          path: build/Build/Products/Debug-iphonesimulator/*

  build-desktop-macos:
    name: Build Desktop macOS
    runs-on: macos-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build macOS DMGs
        run: ./gradlew :sample:ktor:jvmApp:packageDmg :sample:okhttp:jvmApp:packageDmg :sample:http4k:jvmApp:packageDmg

#      - name: Upload macOS DMGs
#        uses: actions/upload-artifact@v7
#        with:
#          name: desktop-macos-dmg
#          path: "**/build/compose/binaries/main/dmg/*.dmg"

  build-desktop-windows:
    name: Build Desktop Windows
    runs-on: windows-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5.0.0

      - name: Build Windows MSIs
        run: ./gradlew :sample:ktor:jvmApp:packageMsi :sample:okhttp:jvmApp:packageMsi :sample:http4k:jvmApp:packageMsi

#      - name: Upload Windows MSIs
#        uses: actions/upload-artifact@v7
#        with:
#          name: desktop-windows-msi
#          path: "**/build/compose/binaries/main/msi/*.msi"

  build-desktop-linux:
    name: Build Desktop Linux
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build Linux DEBs and RPMs
        run: ./gradlew :sample:ktor:jvmApp:packageDeb :sample:ktor:jvmApp:packageRpm :sample:okhttp:jvmApp:packageDeb :sample:okhttp:jvmApp:packageRpm :sample:http4k:jvmApp:packageDeb :sample:http4k:jvmApp:packageRpm

#      - name: Upload Linux DEBs and RPMs
#        uses: actions/upload-artifact@v7
#        with:
#          name: desktop-linux-deb
#          path: |
#            **/build/compose/binaries/main/deb/*.deb
#            **/build/compose/binaries/main/rpm/*.rpm
